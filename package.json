{
  "name": "myblog-1",
  "version": "1.0.0",
  "description": "* 从0 开始搭建，不使用任何框架 * 使用nodemon监测文件变化，自动重启node * 使用cross-env设置环境变量，兼容mac linux and window",
  "main": "www.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev nodemon ./bin/www.js",
    "prd": "cross-env NODE_ENV=production pm2 ./bin/www.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^6.0.3",
    "nodemon": "^2.0.2"
  }
}
